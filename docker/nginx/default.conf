# PHP-FPM runs in the "app" service — reference by service name, not 127.0.0.1
upstream fastcgi_backend {
    server app:9000;
}

server {
    listen 80;
    server_name _;

    set $MAGE_ROOT /var/www/html;
    root $MAGE_ROOT/pub;
    index index.php;
    autoindex off;
    charset UTF-8;

    error_page 404 403 = /errors/404.php;

    location /.user.ini { deny all; }
    location /app/      { deny all; }
    location /lib/      { deny all; }
    location /vendor/   { deny all; }

    location /static/ {
        location ~ ^/static/version\d*/ {
            rewrite ^/static/version\d*/(.*)$ /static/$1 last;
        }
        location ~* \.(ico|jpg|jpeg|png|gif|svg|svgz|js|css|eot|ttf|otf|woff|woff2|webmanifest)$ {
            add_header Cache-Control "public";
            expires +1y;
            # Developer mode: generate on-the-fly when file doesn't exist yet
            if (!-f $request_filename) {
                rewrite ^/static/(version\d*/)?(.*)$ /static.php?resource=$2 last;
            }
        }
        # Fallback for any other static asset type
        if (!-f $request_filename) {
            rewrite ^/static/(version\d*/)?(.*)$ /static.php?resource=$2 last;
        }
    }

    location /media/ {
        try_files $uri $uri/ /get.php$is_args$args;
    }

    location / {
        try_files $uri $uri/ /index.php$is_args$args;
    }

    location ~ ^/(index|get|static|errors/report|errors/404|errors/503|health_check)\.php$ {
        try_files $uri =404;
        fastcgi_pass fastcgi_backend;
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
        fastcgi_read_timeout 600s;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~* (\.php$|\.phtml$|\.htaccess$) { deny all; }
}
